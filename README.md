# Vault

Shared swarm brain on disk + git. Not chat history.

1. Pick SCOPE from `catalog.md`
2. Touch only that path
3. Log when the job is done

Do not commit secrets. Do not create the GitHub remote here — Ivan runs `git remote add` after APPROVE MAP.
